import { describe, expect, it } from 'vitest'

/**
 * Placeholder that fixes where unit tests live: `src/tests/**\/*.test.ts`, run in Node.
 * The real occupants of this folder are the player controller and `sendContactMessage`.
 */
describe('unit test harness', () => {
	it('collects tests from src/tests', () => {
		expect(true).toBe(true)
	})
})
